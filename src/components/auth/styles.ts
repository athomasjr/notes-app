import styled from "styled-components"

export const AuthContainer = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 100%;
  gap: 8px;
`

export const ToggleAction = styled.p`
  font-size: 1.6rem;
  justify-self: left;

  span {
    cursor: pointer;
    color: #0000ff;
  }
`
